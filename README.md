# ppm2ASCII
A simple ppm2ASCII converter written in C

## HOW TO RUN:

compile:

```gcc main.c```

run:

```./a.out```

If you wish to change your image, go to the ```readImage()``` function and change the input ppm file.

